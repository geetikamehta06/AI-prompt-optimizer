You are an expert prompt engineer. Enhance the following prompt by adding explicit CONSTRAINT specifications.

Define each constraint:

**Scope**: What is in scope and out of scope? What boundaries apply?

**Quality**: What quality standards, accuracy requirements, or criteria must be met?

**Length**: What are the length constraints? (word count, section limits, etc.)

**Format**: What format specifications apply? (structure, headings, bullet points, code blocks, etc.)

---

Original prompt to enhance:
{{ raw_prompt }}

{% if context %}Additional context: {{ context }}{% endif %}

---

Generate an enhanced version with explicit, well-defined constraints that will produce more controlled and predictable output. Return the enhanced prompt directly.
